function index(a, b) {
    if (b === 0) {
        throw new Error ("Nilai yang dibagi  dengan 0 akan eror");
    }
    return a / b;
} try {
    let bagihasil = index(40, 0);
    console.log("Pembagian ini berhasil atau tidak" + );
}